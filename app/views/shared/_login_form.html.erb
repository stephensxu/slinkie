<%= form_for(:user, :url => login_path, :method => :post) do |f| %>
  Login with Email <%= f.text_field(:email, placeholder: "Email")%>
  Password <%= f.password_field(:password, placeholder: "password")%>
  <%= f.submit "Login", :style => "width:100px" %>
<% end %>
